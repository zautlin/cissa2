---
phase: 01-backend-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "requirements.txt",
  "setup.py",
  "db/docker-compose.yml",
  "db/.env.local",
  "db/schema/migrations/000_complete_schema.sql",
  "src/__init__.py",
  "src/config/__init__.py",
  "src/config/parameters.py",
  "src/api/__init__.py",
  "src/api/main.py",
  "src/api/models.py",
  "src/api/handlers.py",
  "src/services/__init__.py",
  "src/services/metrics_worker.py",
  "data/Bloomberg Download data.xlsx"
]
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "PostgreSQL database container starts with `docker-compose up -d`"
    - "All 18 database tables and schemas are created automatically"
    - "Python dependencies install without errors"
    - "API can start with `python -m src.api.main`"
    - "API health check endpoint responds with database status"
    - "Sample Bloomberg data file is present in data/ directory"
  artifacts:
    - path: "requirements.txt"
      provides: "All Python dependencies with pinned versions"
      contains: ["fastapi", "psycopg2-binary", "sqlalchemy", "pydantic"]
    - path: "setup.py"
      provides: "Package installation configuration"
      exports: ["cissa"]
    - path: "db/docker-compose.yml"
      provides: "PostgreSQL container orchestration"
      version: "postgres:15.13-alpine"
    - path: "db/schema/migrations/000_complete_schema.sql"
      provides: "Complete USR schema with 18 tables"
      tables: 18
    - path: "src/config/parameters.py"
      provides: "Environment-based configuration"
      env_vars: ["DB_HOST", "DB_PORT", "DB_NAME", "DB_USER", "DEPLOYMENT_MODE"]
    - path: "src/api/main.py"
      provides: "FastAPI application entry point"
      endpoints: ["/health", "/api/metrics", "API routes"]
  key_links:
    - from: "db/docker-compose.yml"
      to: "db/schema/migrations/000_complete_schema.sql"
      via: "Docker volume mount /docker-entrypoint-initdb.d"
      pattern: "volumes.*migrations.*docker-entrypoint-initdb.d"
    - from: "src/api/main.py"
      to: "src/config/parameters.py"
      via: "Database URL construction"
      pattern: "from src.config.parameters import.*DATABASE_URL"
    - from: "src/api/handlers.py"
      to: "src/config/parameters.py"
      via: "Connection string from config"
      pattern: "create_engine.*from.*parameters"

---

<objective>
Integrate the backend database infrastructure and API framework from basos-ds into the CISSA MVP, creating a fully functional database layer with API testing capability.

Purpose: Establish the foundation for data ingestion and API interactions. This plan copies production-ready code (database schema, API framework, services) from the legacy basos-ds project and makes it work in the cissa repository with proper package structure.

Output: Working PostgreSQL database with 18 tables, FastAPI application with health checks, and basic API framework ready for testing.
</objective>

<execution_context>
@/Users/jparnell/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/jparnell/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/INTEGRATION_ANALYSIS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/STACK.md
@.planning/codebase/INTEGRATIONS.md

# Reference existing implementations
@/Users/jparnell/rozettatechnology/basos-ds/backend/requirements.txt
@/Users/jparnell/rozettatechnology/basos-ds/backend/db/docker-compose.yml
@/Users/jparnell/rozettatechnology/basos-ds/backend/db/schema/migrations/000_complete_schema.sql
@/Users/jparnell/rozettatechnology/basos-ds/backend/src/config/parameters.py
@/Users/jparnell/rozettatechnology/basos-ds/backend/src/api/main.py
@/Users/jparnell/rozettatechnology/basos-ds/backend/src/api/models.py
@/Users/jparnell/rozettatechnology/basos-ds/backend/src/api/handlers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Python package structure and dependencies</name>
  <files>requirements.txt, setup.py, src/__init__.py</files>
  <action>
1. **Create requirements.txt** - Copy from `/Users/jparnell/rozettatechnology/basos-ds/backend/requirements.txt` exactly. Contains 18 pinned dependencies including fastapi==0.104.1, psycopg2-binary==2.9.11, sqlalchemy==2.0.46, pydantic==2.5.0, and others.

2. **Create setup.py** - Create minimal setup.py with:
   - name="cissa"
   - version="0.1.0"
   - description="CISSA: Company Valuation Methodology with Portfolio Optimization"
   - packages=find_packages()
   - python_requires=">=3.14"
   - install_requires read from requirements.txt
   - entry_points for CLI: cissa=src.api.main:main (optional for Phase 2)

3. **Create src/__init__.py** - Create empty __init__.py to make src a package. Add docstring: "CISSA backend package for valuation and portfolio optimization".

4. **Verify package structure** - After creating files, verify:
   - `pip install -e .` succeeds (editable install)
   - `python -c "import src; print(src.__file__)"` works
   - All imports from requirements.txt are available
  </action>
  <verify>
    <automated>
pip install -e . && python -c "import src; from src.config import parameters; print('Package setup OK')"
    </automated>
  </verify>
  <done>Package structure complete, requirements installed, src is importable</done>
</task>

<task type="auto">
  <name>Task 2: Copy and configure database infrastructure</name>
  <files>db/docker-compose.yml, db/.env.local, db/schema/migrations/000_complete_schema.sql</files>
  <action>
1. **Create db directory structure:**
   - mkdir -p db/schema/migrations
   - Copy docker-compose.yml from basos-ds/backend/db/ to db/
   - Verify POSTGRES_DB=cissa (correct), POSTGRES_USER=postgres, POSTGRES_PASSWORD=postgres
   - The volumes section should mount ./schema/migrations:/docker-entrypoint-initdb.d (verify this is correct in source)

2. **Create db/.env.local:**
   ```
   POSTGRES_USER=postgres
   POSTGRES_PASSWORD=postgres
   POSTGRES_DB=cissa
   POSTGRES_PORT=5432
   ```
   This file will be used by docker-compose.yml for the local PostgreSQL container.

3. **Copy database schema:**
   - Copy basos-ds/backend/db/schema/migrations/000_complete_schema.sql to db/schema/migrations/
   - This is the authoritative schema file (906 lines) that creates all 18 tables, views, indexes, and functions
   - File should not be modified - it's production schema

4. **Test Docker setup (DO NOT START YET - just verify files):**
   - Verify all three files exist in correct locations
   - Check that docker-compose.yml references ./schema/migrations correctly
   - Confirm 000_complete_schema.sql is readable and has expected content (should start with CREATE SCHEMA IF NOT EXISTS "USR")
  </action>
  <verify>
    <automated>
test -f db/docker-compose.yml && test -f db/.env.local && test -f db/schema/migrations/000_complete_schema.sql && grep -q 'CREATE SCHEMA IF NOT EXISTS "USR"' db/schema/migrations/000_complete_schema.sql && echo "Database files OK"
    </automated>
  </verify>
  <done>Database docker-compose and schema files in place, ready for container startup</done>
</task>

<task type="auto">
  <name>Task 3: Copy and adapt configuration module</name>
  <files>src/config/__init__.py, src/config/parameters.py</files>
  <action>
1. **Create src/config/__init__.py** - Empty init file to make config a package.

2. **Copy and FIX src/config/parameters.py:**
   - Start by copying from basos-ds/backend/src/config/parameters.py
   - FIX ISSUE: Lines 54-57 have hardcoded production values that override env vars:
     ```python
     SERVER = "cissa-dev-postgres.cr9zt2sgd9dw.ap-southeast-2.rds.amazonaws.com"
     DB = "cissa"
     USER = "postgres"
     PORT = "5432"
     ```
   - DELETE these lines (54-57). They override the environment-based logic above.
   - Verify logic flow: DEPLOYMENT_MODE env var → local (localhost) or production (RDS endpoint from env vars)
   - Keep all column mappings (BASE_COLUMN_MAPPING, MONTHLY_DATA_COLUMNS, etc.) - these are correct
   - Keep all worksheet names (DOWNLOADED_WORK_BOOKS, USR_DEFINED_WORK_BOOKS) - these are correct
   - Keep BEGIN_ROW, MONTHLY_END_ROW, ANNUAL_END_ROW constants

3. **Add new env var fallbacks if missing:**
   - Ensure DB_PASSWORD is handled (should read from env var or default)
   - The connection string PASSWORD is hardcoded to "postgres" in api/handlers.py - we'll fix that in next task

4. **Test configuration loading:**
   - DEPLOYMENT_MODE=local should set SERVER=localhost
   - DEPLOYMENT_MODE=production should read DB_HOST, DB_USER, etc. from env
  </action>
  <verify>
    <automated>
export DEPLOYMENT_MODE=local && python -c "from src.config.parameters import SERVER, DB, USER, PORT; print(f'{SERVER}:{PORT}/{DB} as {USER}'); assert SERVER == 'localhost', 'Local mode failed'" && echo "Config OK"
    </automated>
  </verify>
  <done>Configuration module fixed, environment-based settings working, hardcoded production values removed</done>
</task>

<task type="auto">
  <name>Task 4: Copy API framework and handlers</name>
  <files>src/api/__init__.py, src/api/main.py, src/api/models.py, src/api/handlers.py</files>
  <action>
1. **Create src/api/__init__.py** - Copy from basos-ds/backend/src/api/__init__.py (minimal file, just exports).

2. **Copy src/api/models.py** - Copy from basos-ds exactly. Contains Pydantic models for API requests/responses (122 lines). No changes needed.

3. **Copy src/api/handlers.py** - Copy from basos-ds exactly. Contains all endpoint handler functions (649 lines). Contains DatabaseConnection class and all route logic. No changes needed at this stage.

4. **Copy src/api/main.py** - Copy from basos-ds but with ONE FIX:
   - Line 17 constructs DATABASE_URL from hardcoded "postgres" password:
     ```python
     DATABASE_URL = f"postgresql://{USER}:postgres@{SERVER}:{PORT}/{DB}"
     ```
   - Change this to read from env var with fallback:
     ```python
     from src.config.parameters import USER, SERVER, PORT, DB
     DB_PASSWORD = os.getenv("DB_PASSWORD", "postgres")  # Add import: import os
     DATABASE_URL = f"postgresql://{USER}:{DB_PASSWORD}@{SERVER}:{PORT}/{DB}"
     ```
   - This allows flexible password management while defaulting to "postgres" for local development

5. **Verify imports in all files:**
   - All references to src.config.parameters should work
   - All references to src.engine, src.services, src.utils should work (these will be imported but may not exist yet - OK for now)
   - Handler imports from sqlalchemy, fastapi should work (dependencies in requirements.txt)

6. **Check API endpoints** - Don't modify them, just verify they exist:
   - /health - health check
   - /api/metrics - metrics endpoints
   - Other routes in handlers.py
  </action>
  <verify>
    <automated>
python -c "from src.api.main import app; from src.api.models import *; from src.api.handlers import *; print('API imports OK')" && grep -q "DATABASE_URL" src/api/main.py && echo "API framework OK"
    </automated>
  </verify>
  <done>API framework copied, database URL construction fixed to use env vars</done>
</task>

<task type="auto">
  <name>Task 5: Copy services module for async metrics processing</name>
  <files>src/services/__init__.py, src/services/metrics_worker.py</files>
  <action>
1. **Create src/services/__init__.py** - Copy from basos-ds (minimal file).

2. **Copy src/services/metrics_worker.py** - Copy from basos-ds exactly. This is the async worker that processes metrics jobs in the background. API's lifespan handler will instantiate this. No changes needed.

3. **Verify imports** - metrics_worker.py imports from src.config.parameters and src.engine - these will be resolved in later steps.

4. **Note:** This is a dependency of api/main.py's lifespan handler. It won't actually run metrics yet (engine modules not present), but the framework will be in place.
  </action>
  <verify>
    <automated>
python -c "from src.services.metrics_worker import create_metrics_worker; print('Services imports OK')" && test -f src/services/__init__.py && echo "Services module OK"
    </automated>
  </verify>
  <done>Services module in place, metrics worker framework ready</done>
</task>

<task type="auto">
  <name>Task 6: Copy sample data file</name>
  <files>data/Bloomberg Download data.xlsx</files>
  <action>
1. **Create data directory:**
   - mkdir -p data

2. **Copy Bloomberg data file:**
   - Copy from /Users/jparnell/rozettatechnology/basos-ds/data/Bloomberg Download data.xlsx to data/
   - This file is required by data ingestion workflows (src/upload_data_to_db.py)
   - It contains worksheets: Base, Company TSR, Index TSR, Rf, FY Dates, FY Period, FY TSR, Spot Shares, Share Price, MC, PAT, Total Equity, MI, Div, Revenue, Op Income, PBT, PAT XO, Total Assets, Cash, FA, GW
   - File size will be large (Excel with financial data) - this is OK

3. **Verify file presence:**
   - File should be at least 1MB (typical Excel with Bloomberg data)
   - File should be readable (no corruption)
  </action>
  <verify>
    <automated>
test -f data/'Bloomberg Download data.xlsx' && test -s data/'Bloomberg Download data.xlsx' && echo "Data file OK: $(du -h data/'Bloomberg Download data.xlsx')"
    </automated>
  </verify>
  <done>Sample Bloomberg data file present in data/ directory</done>
</task>

<task type="auto">
  <name>Task 7: Verify complete integration and create startup guide</name>
  <files>.planning/STARTUP_GUIDE.md</files>
  <action>
1. **Run final integration checks:**
   - Python environment: `python --version` should be 3.14+
   - Package structure: `python -m src.api.main --help` should not error on imports (may error on missing services - OK)
   - Dependencies: `pip list | grep fastapi` should show fastapi==0.104.1
   - Database files: All db/ files in place
   - Data files: Bloomberg data present

2. **Create .planning/STARTUP_GUIDE.md with instructions:**
   - How to start the PostgreSQL container
   - How to verify database is ready
   - How to start the API server
   - How to test the health endpoint
   - Common troubleshooting
   - Next steps for Phase 2

3. **Document completion state:**
   - What's working (database schema, API framework, configuration)
   - What's not yet working (data ingestion, metrics calculation - these require engine modules)
   - What's next (Phase 2: copy engine modules for data pipeline)

4. **Create simple health check script (optional but helpful):**
   - Create a test script that can verify all components are in place
   - Run `python test_integration.py` to verify everything
  </action>
  <verify>
    <automated>
test -d db && test -d src && test -d data && test -f requirements.txt && test -f setup.py && python -c "from src.config import parameters; print('Config OK'); from src.api import main; print('API OK')" && echo "Integration verified"
    </automated>
  </verify>
  <done>Complete backend infrastructure in place, startup guide created, integration verified</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. **Package Installation:**
   - `pip install -e .` completes without errors
   - `python -c "import src"` works

2. **Database Setup:**
   - `ls db/docker-compose.yml db/.env.local db/schema/migrations/000_complete_schema.sql` all exist
   - 000_complete_schema.sql contains "CREATE SCHEMA" and "18" appears in comments

3. **API Framework:**
   - `python -c "from src.api.main import app; print(app)"` shows FastAPI app
   - src/api/main.py has DATABASE_URL that uses env vars
   - src/api/handlers.py is present and readable

4. **Configuration:**
   - DEPLOYMENT_MODE=local → SERVER="localhost"
   - Hardcoded production endpoints removed from parameters.py
   - All environment variables fallback correctly

5. **Data Files:**
   - data/Bloomberg Download data.xlsx exists and is >1MB

6. **File Structure:**
   ```
   cissa/
   ├── requirements.txt
   ├── setup.py
   ├── db/
   │   ├── docker-compose.yml
   │   ├── .env.local
   │   └── schema/migrations/000_complete_schema.sql
   ├── src/
   │   ├── __init__.py
   │   ├── config/
   │   │   ├── __init__.py
   │   │   └── parameters.py
   │   ├── api/
   │   │   ├── __init__.py
   │   │   ├── main.py
   │   │   ├── models.py
   │   │   └── handlers.py
   │   └── services/
   │       ├── __init__.py
   │       └── metrics_worker.py
   └── data/
       └── Bloomberg Download data.xlsx
   ```
</verification>

<success_criteria>
- ✅ All 18 required files copied and created
- ✅ requirements.txt has all 18 dependencies with pinned versions
- ✅ setup.py allows `pip install -e .` to succeed
- ✅ PostgreSQL container definition in docker-compose.yml
- ✅ Database schema file (000_complete_schema.sql) in migrations/
- ✅ Configuration uses environment variables (DEPLOYMENT_MODE, DB_HOST, DB_PASSWORD, etc.)
- ✅ Hardcoded production values removed from parameters.py
- ✅ API framework (FastAPI app with handlers) in place
- ✅ Bloomberg sample data file present in data/
- ✅ All Python files pass basic import checks
- ✅ STARTUP_GUIDE.md explains next steps
</success_criteria>

<output>
After execution, create `.planning/phases/01-backend-infrastructure/01-01-SUMMARY.md` documenting:
- Files created and integrated
- Configuration changes made
- Database schema location and structure
- API endpoints available
- Next steps (Phase 02: Copy engine modules for data ingestion)
- How to start PostgreSQL and test API
</output>
